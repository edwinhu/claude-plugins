# Moved

This file has moved to the shared reference location:

Read `${CLAUDE_SKILL_DIR}/../../references/enforcement-checklist.md`.

The enforcement checklist is a shared resource used by multiple skills (skill-creator, workflow-creator, dev-debug, etc.) and belongs in `references/`, not under any single skill.
