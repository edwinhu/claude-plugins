---
description: Query WRDS PostgreSQL for Compustat, CRSP, and other financial data
allowed-tools: ["Skill"]
---

Invoke the `workflows:wrds` skill for WRDS query patterns.

Use the Skill tool with `skill: "workflows:wrds"` to load the full instructions.
