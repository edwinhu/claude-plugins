---
description: Start the development workflow with TDD enforcement and sandbox mode
allowed-tools: ["Skill"]
---

Invoke the `workflows:dev` skill to begin the development workflow.

Use the Skill tool with `skill: "workflows:dev"` to load the full workflow instructions.
