# Section execution

The plan must enumerate concrete section outputs and preserve the deterministic section index. Each
section task names its destination, neighboring-outline context needed for transitions, domain style
constraints, and execution-fidelity evidence. Dependencies must distinguish genuinely independent
sections from transitions or shared inputs that require ordering.
