# Generation and assembly

The plan must preserve section-level generation, exclusive fragment paths, independent inventory
probes, an explicit assembly barrier, and compilation of both `slides.typ` and `notes.typ`. Structural
or inventory changes require plan replacement rather than an implementation-time deviation.
