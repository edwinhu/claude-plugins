# Actionability

Each task must be concrete, bounded, and executable by its assigned worker: name its intended output or other affected artifact when that is material, a specific action, expected result, and completion evidence. Vague verbs, placeholders, or unbounded bundled work are blockers. Domain constraints determine format-specific file and task-table requirements.
