# RED-first TDD and verification commands

Each behavioral task needs a meaningful failing RED test before implementation, a concrete task-local verify command, and a full-suite command where applicable. Placeholder commands or tests that cannot fail for the intended change are blocking.
